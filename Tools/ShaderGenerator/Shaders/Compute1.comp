#version 450

layout(local_size_x = 16, local_size_y = 16, local_size_z = 2) in;

layout(set = 0, binding = 0, rgba32f) uniform writeonly image3D img;  

void main()
{
    imageStore(img, ivec3(gl_GlobalInvocationID.xyz), vec4(0, 1, 0, 1));
}